/** @type {import('next').NextConfig} */
const nextConfig = {
  // Next.js в dev-режиме по умолчанию блокирует запросы не с "своего" origin
  // (это защита от DNS-rebinding, а не баг) — поэтому без явного разрешения
  // сюда, вход извне через переадресацию портов на Keenetic выглядит как
  // "форма есть, но не работает" (JS не догружается/не хайдрируется).
  allowedDevOrigins: [
    '192.168.1.41',       // ← LAN IP машины (из терминала)
    '109.194.14.119',     // ← внешний (публичный) IP от Дом.ру
  ],
};

export default nextConfig;