# План: Переезд на локальный сервер (Mac mini)

## Статус: Актуальная версия перенесена

Полный актуальный план (автозапуск, Tailscale, Screen Sharing, бэкапы в GitHub, Keenetic / `tradecom.keenetic.link`):

→ **[переход_на_mac_mini.plan.md](./переход_на_mac_mini.plan.md)**

Копия также в `~/.cursor/plans/переход_на_mac_mini_e4c0bcd2.plan.md`.

Старый черновик от 25.07.2026 здесь больше не ведётся — правки только в `переход_на_mac_mini.plan.md`.

### Кроны — обязательно при cutover

> На локальном сервере выставить **задуманные** интервалы для **всех** jobs.  
> Не копировать урезанное расписание с Vercel Hobby.

Полная таблица: [`scripts/cron-schedules.md`](../../scripts/cron-schedules.md)  
Строки crontab: план Mac mini, **Фаза 4**.

| Среда | GPS СКАУТ (`scout-sync` каждые 2 мин) | Остальные кроны |
|-------|--------------------------------------|-----------------|
| **Vercel (сейчас)** | нет в `vercel.json` (Hobby); localCrons / Mac | UTC в `vercel.json` |
| **Ноутбук `next dev`** | `lib/localCrons.ts` | вручную / localCrons daily |
| **Mac mini после cutover** | crontab `*/2` + `cron-curl.sh scout-sync`; `ENABLE_LOCAL_CRONS=0` | полный crontab МСК из Фазы 4 (включая `scout-sensors-daily` 23:50) |
